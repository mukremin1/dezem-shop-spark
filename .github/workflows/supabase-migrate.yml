name: CI: Migrate & Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

    - name: Use Node.js
        uses: actions/setup-node@v4.11.0
        with:
           node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Link Supabase project (non-interactive)
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          npx supabase@latest link --project-ref $SUPABASE_PROJECT_REF

      - name: Run Supabase migrations
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          npx supabase@latest db push --include-all --debug

      - name: Build site
        run: npm run build

      - name: Upload built site for Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./dist   # <-- Eğer build çıktın dist/ veya out/ ise burayı değiştir

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
